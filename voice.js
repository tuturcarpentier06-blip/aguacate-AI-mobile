window.AguacateVoice={toggle(input,toast){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR)return toast('🎙️ La dictée vocale n’est pas disponible sur ce navigateur.');if(this.rec){this.rec.stop();this.rec=null;return}const r=new SR();r.lang='fr-FR';r.interimResults=true;r.continuous=false;r.onresult=e=>{let s='';for(const x of e.results)s+=x[0].transcript;input.value=s;input.dispatchEvent(new Event('input'))};r.onerror=()=>toast('🎙️ Impossible d’utiliser le micro.');r.onend=()=>{this.rec=null};this.rec=r;r.start();toast('🎙️ Parle…')}};
